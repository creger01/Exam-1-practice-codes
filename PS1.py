# the authors names are: Cadyn Reger
# Dene a function that takes the two parameters width and length of a rectangle
#and returns its area. Check if your function works correctly by passing 2 and 10 as its arguments.
def rectangle(width,length):
    area = width * length
    return area

rectangle(2,10)
